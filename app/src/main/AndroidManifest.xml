<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.loonspartypack">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Game3.EndGame"></activity>
        <activity android:name=".Game3.Game3Activity" />
        <activity android:name=".Game2.Game2Activity" />
        <activity
            android:name=".Game1.TicTacToe"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/SplashTheme">

            <!-- Created a new style that removes the action bar from the splash screen -->
            <intent-filter>

                <!-- Sets splash screen as the first activity to be opened -->
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Game4.MathGameDisplay" />
        <activity android:name=".Game4.MathGamePlay" />
        <activity android:name=".Game4.MathGamePlayAgain" />
        <activity android:name=".Game4.MathGameWon" />
    </application>

</manifest>